[build]
  base = "travel-web"
  command = "npm install lightningcss-linux-x64-gnu@1.32.0 --no-save && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=optional"
